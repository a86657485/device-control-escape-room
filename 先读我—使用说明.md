# 机关密室：设备控制逃脱行动

六年级信息科技《设备控制处处在》配套。6间密室、18个操作任务、6个解释取证、5枚晶石与最终出口。旧版已由教师带班使用，反馈完成过快；本版已改为情境探究与分组考核，可随时离开并继续。

## 直接打开

双击 `index.html`，用 Chrome、Edge 或其他现代浏览器打开。图片、程序已内嵌，复制这一个文件也能离线玩。建议两人一组，一人操作、一人观察，每间交换角色。

流程：先读任务 → 找设备/观察热点 → 选择预测 → 操作控制 → 启动试运行 → 根据结果调整 → 记录发现 → 解释取证。没有倒计时惩罚。每个任务独立提供观察线索、思考方向、具体帮助三级提示；最后一级需先做一次试运行。声音默认关闭，所有线索都可通过文字与画面获得。

## 电脑教室局域网访问

1. 将整个项目文件夹放在教师电脑，教师电脑需安装 Python 3；学生电脑不需要安装。
2. macOS 双击 `启动局域网.command`；Windows 双击 `启动局域网-Windows.bat`。
3. 保持启动窗口开启。它会显示教师本机地址与可尝试的局域网地址，例如 `http://192.168.1.100:8765`。
4. 学生电脑与教师电脑在同一个可互访的网络，浏览器输入窗口中显示的地址。局域网不需要互联网。
5. 关闭启动窗口或按 Control+C 停止服务。不要把教师电脑的 localhost/127.0.0.1 地址发给学生。

曾在制作环境验证本机地址及本机经局域网IP的响应；未在实际电脑教室学生设备上测试。更换网络后，IP可能改变，以启动窗口显示为准。

如果 macOS 拒绝直接运行脚本，可在终端进入项目文件夹后执行 `python3 启动服务.py`。如果端口被占用，执行 `python3 启动服务.py --port 8766`，学生也需改为8766端口。

学生无法访问但教师本机能打开时，请检查双方是否在同一网络、网络是否启用客户端隔离、系统是否允许程序接收局域网连接。可先复制 index.html 离线使用。Windows启动脚本经过静态检查，本次未在Windows电脑双击实测。

## 学习记录与导出

进度、预测、尝试、解释、终局任务单考核和机关卡只保存在当前学生浏览器。不同电脑、浏览器、端口，以及离线文件与局域网地址之间，不会自动同步。教师端没有集中统计页面。

通关后完成“出口档案考核”：分四组完成16道情境题（每组4题），再从四组选项拼出迁移表达，不要求输入长段文字。在线索簿填写小组代号后点击“导出闯关报告”。得到可离线打开、可打印的HTML报告，可通过学校现有教学平台提交。报告包含错误与成功尝试、16题首答与订正、迁移搭配和项目机关证据，不用提示次数或速度替代学习评价。线索簿中的“重新开始”会在确认后清除当前浏览器的本轮记录。

完成主线后可进入“设计同伴挑战”：写机关任务与理由 → 选择风速和定时 → 自己验证 → 同伴在同一台电脑验证 → 留建议并修改。改变任务条件需要重新测试；报告保留之前版本的同伴证据。游戏不进行多人联网同步。

## 配套教学材料

- `学习活动设计—教师版.docx`：可编辑的6页教学设计，含目标、课标依据、项目任务、跨学科活动、课堂流程、评价、支架和部署说明。
- `探险任务单—学生版.docx`：可编辑的2页记录与自创机关任务单。
- 同名PDF：用于查看、投屏或打印。
- `美术素材`：生图原图、网页压缩素材、提示词记录。
- `开发与验证`：源码、构建与测试、实测截图。日常使用不需要打开这一目录。

## 内容与技术说明

对齐用户提供的教材印刷页11—14，课标第三学段“过程与控制”“小型系统模拟”和教学评价建议。本课重在设备功能、控制方法、控制系统初步认识及发展变化，不将后续编程/反馈/逻辑运算列为必过关要求。

模拟设备面板不代表所有真实型号。风扇促进空气流动；游戏风力机关是虚构装置。定时通过快进展示，不真实等待30分钟。机械锁与智能锁不同形式可以并存。公共设备只在模拟界面操作。

采用标准HTML/CSS/JavaScript、内嵌SVG及本地生成美术，无外部库/CDN、无在线AI调用。支持键盘可见焦点、减少动态效果；动画主要使用变换与透明度。减少动态效果的实现依据：[MDN](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion)。素材使用内置image_gen生成，工具未公开模型名称，不能确认指定为image2。
